<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="pste.simon.aucoeurdenozay.Android_File.MainActivity">

    <Button
        android:text="Calendrier"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/Calendar"
        android:layout_centerVertical="true"
        android:layout_alignStart="@+id/New_Order"
        android:layout_alignEnd="@+id/New_Order" />

    <Button
        android:text="Nouvelle Commande"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:id="@+id/New_Order"
        android:layout_marginTop="32dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Gestion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/Settings"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/Calendar"
        android:layout_marginBottom="68dp"
        android:layout_alignEnd="@+id/Calendar" />
</RelativeLayout>
